# OpenMemory Environment Variables

# Database Configuration
# For local development with SQLite:
DATABASE_URL=sqlite:///./openmemory.db
# For production with PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:port/database

# Qdrant Configuration (for vector search)
QDRANT_URL=http://localhost:6333
# For production/DO:
# QDRANT_URL=http://your-qdrant-instance:6333

# User Configuration
USER_ID=default-user
DEFAULT_APP_ID=default-app

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# SIGMA supports both OpenAI directly and OpenRouter (LLM gateway)
# Choose ONE of the following configurations:

# -----------------------------------------------------------------------------
# Option 1: OpenAI Direct (Default)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-key-here
# OPENAI_BASE_URL=  # Leave empty or unset for default OpenAI API

# -----------------------------------------------------------------------------
# Option 2: OpenRouter (Multi-provider LLM Gateway)
# -----------------------------------------------------------------------------
# OpenRouter provides access to 100+ models from OpenAI, Anthropic, Google, etc.
# Get your API key at: https://openrouter.ai/keys
#
# To use OpenRouter:
# 1. Set OPENAI_API_KEY to your OpenRouter API key
# 2. Set OPENAI_BASE_URL to OpenRouter's endpoint
#
# OPENAI_API_KEY=sk-or-v1-your-openrouter-key-here
# OPENAI_BASE_URL=https://openrouter.ai/api/v1
#
# Popular OpenRouter models (use in config.json):
# - openai/gpt-4o-mini          (fast, cheap)
# - openai/gpt-4o               (powerful)
# - anthropic/claude-3.5-sonnet (excellent for code)
# - anthropic/claude-3-haiku    (fast, cheap)
# - google/gemini-pro-1.5       (long context)
# - meta-llama/llama-3.1-70b-instruct (open source)
#
# Note: For embeddings, OpenRouter supports OpenAI embedding models.
# If using a non-OpenAI model for chat, you may still need OpenAI for embeddings.

# Application Configuration
ENVIRONMENT=local
PORT=8000
BIND_PUBLIC_HOST=True
LOG_LEVEL=INFO
JSON_LOGGING_ENABLED=False
DISABLE_HEALTH_CHECK_ACCESS_LOGS=False

# AWS Secrets Manager (optional - for AWS deployments)
# AWS_REGION=us-east-1
# AWS_DEFAULT_REGION=us-east-1
# DB_SECRET_NAME=prod/openmemory/db

# Slack Integration (optional)
# SLACK_BOT_TOKEN=xoxb-your-bot-token
# SLACK_APP_TOKEN=xapp-your-app-token

# Datadog (optional)
DD_TRACE_ENABLED=False
DD_ENV=local
DD_VERSION=0.0.0


